<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gym Code 1337 - Profile</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom Styles -->
    <style>
        body {
            background-color: #f7f7f7;
        }
        .profile-container {
            margin-top: 100px;
            max-width: 600px;
            background-color: #ffffff;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .profile-title {
            color: #ff0000; /* Red color */
            text-align: center;
            margin-bottom: 20px;
        }
        .profile-info {
            font-size: 18px;
            color: #333;
            margin-bottom: 15px;
        }
        .profile-info strong {
            color: #28a745; /* Green color */
        }
    </style>
</head>
<body>

    <div class="container">
        <div class="profile-container mx-auto">
            <h2 class="profile-title">Profile Information</h2>
            
            <!-- Jinja2 Template for Dynamic Data -->
            <p class="profile-info"><strong>Name:</strong> {{ name }}</p>
            <p class="profile-info"><strong>Email:</strong> {{ email }}</p>
            <p class="profile-info"><strong>Is admin:</strong> {{ role }}</p>
        </div>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
